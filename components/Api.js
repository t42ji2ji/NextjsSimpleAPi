import "isomorphic-fetch"





const api = {
    getApires(app){
        api.getApires()
        fetch(`http://140.115.197.16/?school=fcu&app=${app}`)
        .then(
            setTimeout(function() { window.location = webs[num%11]}.bind(this), 2000)
        )
    }
};

module.exports = api;